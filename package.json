{
  "name": "contact-form-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate-forms.mjs",
    "predev": "npm run generate",
    "dev": "wrangler dev",
    "predeploy": "npm run generate",
    "deploy": "wrangler deploy",
    "pretest": "npm run generate",
    "test": "vitest run",
    "pretest:watch": "npm run generate",
    "test:watch": "vitest",
    "pretypecheck": "npm run generate",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.3",
    "wrangler": "^4.0.0"
  }
}
