// Form configuration is data, not code: it lives in forms/*.json and is compiled
// into src/forms.generated.ts by `npm run generate` (runs automatically before
// typecheck/test/dev/deploy). Add a website = add a forms/<id>.json file.
export { FORMS } from './forms.generated'
